/** @type {import('next').NextConfig} */
const nextConfig = {
}

module.exports = nextConfig

// const nextConfig = {
//     output: 'export',
//     generateBuildId: async () => {
//         return 'build-' + Date.now()
//     }
// }